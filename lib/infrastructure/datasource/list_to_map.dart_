/// Пренобразует List в Map
class ListToMap {
  final List _list;
  ListToMap({
    required List list,
  }):
    _list = list;
  Map convert() {
    final Map data = _list.asMap().map(
      (key, value) => MapEntry('$key', value),
    );
    return data;
  }
}
