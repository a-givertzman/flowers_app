import 'package:flutter/material.dart';

class CommonUiSettings {
  static const flushBarDuration = Duration(seconds: 4);
  static const padding = 8.0;
  static const todosSpasing = 8.0;
  static final deleteBgColor = Colors.red[400];
  static final savingInProgressOverlayColor = Colors.black.withOpacity(0.8);
  static const circularProgressIndicatorColor = Colors.blue;
  static const circularProgressIndicatorDuration = Duration(milliseconds: 150);
}